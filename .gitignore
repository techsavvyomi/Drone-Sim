node_modules/
.vite/
out/
dist/
*.log
.DS_Store

# Reference-only legacy project (separate app, large build artifacts)
ar_drone_sim/

# Local planning / AI knowledge-graph output — keep out of remote
docs/
graphify-out/

# Cursor / agent tooling — local only
.cursor/
.claude/
.codex/
AGENTS.md
CLAUDE.md
**/*.canvas.tsx

# 3D model assets (.glb) are committed. Keep every file under GitHub's
# 100MB hard limit; run the optimiser and commit the .opt.glb if one grows.
